server {
    listen 80;

    location / {
        proxy_pass http://frontend:8080;
    }

    location /health {
        proxy_pass http://backend:8001;
    }

    location /api {
        proxy_pass http://backend:8001;
    }
}